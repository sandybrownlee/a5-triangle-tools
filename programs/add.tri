let
  var a : Integer;
  var b : Integer;
  var sum : Integer;
  var product : Integer
in
begin
  a := 0;
  b := 2;
  getint(var b);
  getint(var a);
  
  sum := a + b;
  product := a * b;

  if sum = product
  then put('Y')
  else put('N')
end
