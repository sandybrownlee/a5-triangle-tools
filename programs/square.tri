! for a given input integer a, should print a*a and a*a*a*a 
! Must be less than 14 otherwise overflow

let
    var a: Integer
in
begin
    getint(var a);
    a**;
    putint(a);
    puteol();
    a**;
    putint(a);
    puteol();
end
